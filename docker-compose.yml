version: '3.8'

services:
  app:
    build: .
    container_name: lightweight-ec2-control
    ports:
      - "3000:3000"
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/app/data
